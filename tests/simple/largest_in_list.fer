let io = import( 'std/io' );
let vec = import( 'std/vec' );

let list = vec.new( 5, 1, 2, 4, 3 );

let largest = list[ 0 ];

for item in list {
	if largest < item { largest = item; }
}

io.println( 'largest in ', list, ' is ', largest );
