let io = import( 'std/io' );
let lang = import( 'std/lang' );

let type_t = lang.struct( a = 10, b = 20 );
let f in type_t = fn(x, y, z, .kw, va...) {
	io.println(self.a, self.b);
	io.println('x: ', x);
	io.println('y: ', y);
	io.println('z: ', z);
	io.println('kw: ', kw);
	io.println('va: ', va);
};

let test = type_t( a = 100, b = 200 );
test.f( 1, 2, 3, 4, a = 10, b = 20, c = 30, 5, 6, 7, 8 );

io.println( 'hi there' );
