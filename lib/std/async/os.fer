# Async equivalent (as needed) to std/os module.

let time = import('std/time');

"
  fn(duration) -> Nil
Waits asynchronously for `duration` milliseconds.
"
let sleep = fn(duration) {
    let end = time.now() / 1000 + duration;
    while time.now() / 1000 < end {
        yield;
    }
};