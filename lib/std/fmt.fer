/*
Example:
let res = fmt(`Current data is: {}`, data);
OR
`Current data is: {data}`
The 'data' variable is retrieved using _getVar_() function
which returns the data value or 'nil'.
The string interpolation is done completely in Feral.
*/

# TODO:
let fmt = fn(str, args...) {};