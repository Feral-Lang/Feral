#include "VM/VM.hpp"

namespace fer
{

FERAL_FUNC(moduleGetPath, 0, false,
           "  var.fn() -> Str\n"
           "Returns the path of the module `var`.")
{
    return vm.makeVar<VarStr>(loc, as<VarModule>(args[0])->getPath());
}

} // namespace fer